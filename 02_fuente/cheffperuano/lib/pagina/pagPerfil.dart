import 'package:flutter/material.dart';

class PagPerfil extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Perfil", textScaleFactor: 2.0),
    );
  }
}