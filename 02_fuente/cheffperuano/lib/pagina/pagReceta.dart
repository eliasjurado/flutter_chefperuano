import 'package:flutter/material.dart';

class PagReceta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Receta", textScaleFactor: 2.0),
    );
  }
}