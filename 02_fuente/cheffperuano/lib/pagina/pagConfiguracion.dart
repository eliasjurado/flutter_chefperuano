import 'package:flutter/material.dart';

class PagConfiguracion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Configuracion", textScaleFactor: 2.0),
    );
  }
}