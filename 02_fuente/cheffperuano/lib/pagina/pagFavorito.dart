import 'package:flutter/material.dart';

class PagFavorito extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Favorito", textScaleFactor: 2.0),
    );
  }
}