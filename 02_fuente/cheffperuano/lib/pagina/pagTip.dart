import 'package:flutter/material.dart';

class PagTip extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Tip", textScaleFactor: 2.0),
    );
  }
}